0. (в config/services.yaml добавлена команда delete:old-data)
1. crontab -e
2. ввести в файл
2.1 * * * * * /path/to/php /path/to/bin/concole delete:old-data >> /path/to/log/del-old-data.log 2>&1
2.2 * * * * * - воспроизводство команды каждую минуту.
    0 0 * * 0 - раз в неделю в вск в 00:00
    0 0 1 * * - раз в месяц первого числа в 00:00
3. после второго пункта команда автоматически запустилась и работает.
3.1 для просмотра хода работы команды нужно зайти в лог файл.
3.2 tail -f /path/to/log/del-old-data.log данная команда в терминале отобразит ход работы команды.
